services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq-automl
    ports:
      - "5672:5672" # RabbitMQ broker port
      - "15672:15672" # Management UI
    environment:
      RABBITMQ_DEFAULT_USER: atul1306
      RABBITMQ_DEFAULT_PASS: 12345
